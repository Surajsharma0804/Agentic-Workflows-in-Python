# Render Blueprint for Agentic Workflows
# This file enables one-click deployment to Render.com

services:
  # PostgreSQL Database
  - type: pserv
    name: agentic-workflows-db
    plan: free
    region: singapore
    databaseName: agentic_workflows
    databaseUser: agentic

  # Redis Cache
  - type: redis
    name: agentic-workflows-redis
    plan: free
    region: singapore
    maxmemoryPolicy: allkeys-lru

  # FastAPI Backend
  - type: web
    name: agentic-workflows-api
    env: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: free
    region: singapore
    branch: main
    healthCheckPath: /api/health
    envVars:
      - key: ENVIRONMENT
        value: production
      - key: DEBUG
        value: false
      - key: DATABASE_URL
        fromDatabase:
          name: agentic-workflows-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: agentic-workflows-redis
          type: redis
          property: connectionString
      - key: CELERY_BROKER_URL
        fromService:
          name: agentic-workflows-redis
          type: redis
          property: connectionString
      - key: CELERY_RESULT_BACKEND
        fromService:
          name: agentic-workflows-redis
          type: redis
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: LOG_LEVEL
        value: INFO
